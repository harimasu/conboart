# Copy to .env and fill in. NEVER commit your real .env.
# Leave MESHY_API_KEY blank to run in MOCK mode (generates a sample model,
# no Meshy account needed) while you build the UI.
MESHY_API_KEY=
MESHY_MOCK=1

# Guard rails
MAX_UPLOAD_MB=10
SESSION_TTL_MIN=30
POLL_TIMEOUT_S=180
POLL_INTERVAL_S=3
